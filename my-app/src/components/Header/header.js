import React from "react";
import "./header.css";

const Header = () => (
    <header className = "jumbotron">
        <h1>Tom and Jerry Click Game</h1>
        <h3>Try to avoid clicking the same image</h3>
    </header>
)
export default Header;